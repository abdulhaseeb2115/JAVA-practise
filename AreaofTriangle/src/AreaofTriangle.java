import java.util.*;
public class AreaofTriangle {

	public static void main(String[] args) {
	/*Scanner input=new Scanner(System.in);
	double x1,x2,x3,y1,y2,y3,s,s1,s2,s3,a1,a2,a3,b1,b2,b3,area;
	System.out.println("Enter point 1:");
	x1=input.nextDouble();y1=input.nextDouble();
	System.out.println("Enter point 2:");
	x2=input.nextDouble();y2=input.nextDouble();
	System.out.println("Enter point 3:");
	x3=input.nextDouble();y3=input.nextDouble();
	a1=Math.pow((x2-x1), 2);
	b1=Math.pow((y2-y1), 2);
	
	a2=Math.pow((x2-x3), 2);
	b2=Math.pow((y2-y3), 2);
	
	a3=Math.pow((x3-x1), 2);
	b3=Math.pow((y3-y1), 2);
	
    s1=Math.pow((a1+b1), 0.5);
    s2=Math.pow((a2+b2), 0.5);
    s3=Math.pow((a3+b3), 0.5);
	s=(s1+s2+s3)/2.0;
	
	area=Math.pow((s*(s-s1)*(s-s2)*(s-s3)), 0.5);
	System.out.println("Area of triangle is:"+area);*/
		 Scanner inp=new Scanner(System.in);
		 System.out.print("What do you want to do?");
		 String fn=inp.next();	
	     String Sin="sin";
		 if(fn==Sin) {
	            System.out.println("ni");}
		    else if(fn=="cos") {
	            System.out.println("nope");}
	
	
	}}
